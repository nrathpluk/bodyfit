# CLAUDE.md

คำแนะนำสำหรับ Claude Code (claude.ai/code) เมื่อทำงานกับ repo นี้

## โปรเจกต์นี้คืออะไร

เว็บนับแคลอรีและสารอาหารแบบ MyFitnessPal — เลือกวัตถุดิบ/อาหาร บันทึกลง diary รายวัน
ดูมาโครและไมโครเทียบกับเป้าหมายที่คำนวณจากโปรไฟล์ ใช้งานหลักบน **มือถือ** (PWA)

## ภาษาที่ใช้ในโปรเจกต์

คอมเมนต์ เอกสาร ข้อความ UI ข้อความ error และชื่อ step ใน CI **เขียนเป็นภาษาไทยทั้งหมด**
(ชื่อตัวแปร/ฟังก์ชันเป็นอังกฤษตามปกติ) — เขียนโค้ดใหม่ให้เข้ากับแนวนี้

คอมเมนต์ส่วนใหญ่อธิบาย **"ทำไม"** ไม่ใช่ "ทำอะไร" ถ้าจะแก้โค้ดที่มีคอมเมนต์อธิบายเหตุผลกำกับไว้
ให้อ่านเหตุผลนั้นก่อน — หลายจุดเป็นการตัดสินใจที่ตั้งใจ ไม่ใช่ความพลาด

## คำสั่ง

```bash
npm run dev                 # dev server
npm run typecheck           # tsc --noEmit
npm run lint                # eslint
npm test                    # vitest run
npm run db:generate         # สร้าง migration จาก src/db/schema.ts (ออฟไลน์ได้)
npm run db:migrate          # รัน migration (ต้องมี DATABASE_URL จริง)
```

**ฐานข้อมูล** — migration รันได้สองทาง: `npm run db:migrate` (ต้องมี `DIRECT_URL` ใน `.env.local`)
หรือยิงผ่าน Supabase connector ตอนไม่มีรหัสผ่านฐานข้อมูล ถ้าใช้ทางหลัง **ต้องเขียนแถวลง
`drizzle.__drizzle_migrations` เอง** (hash = sha256 ของทั้งไฟล์ .sql, created_at = ค่า `when`
ใน `meta/_journal.json`) ไม่งั้น `db:migrate` ครั้งถัดไปจะรันซ้ำแล้วพังที่ "ตารางมีอยู่แล้ว"

## Next.js 16 — ไม่เหมือนที่จำมา

- middleware ถูกเปลี่ยนชื่อเป็น **proxy** → ไฟล์คือ `src/proxy.ts` (export default)
- `params` และ `searchParams` เป็น `Promise` ต้อง `await`
- เอกสารของเวอร์ชันที่ติดตั้งจริงอยู่ใน `node_modules/next/dist/docs/` — อ่านที่นั่นก่อนเขียนโค้ดที่ไม่แน่ใจ
- `next dev` จะเขียนบล็อกกฎของ Next.js กลับเข้าไปใน `AGENTS.md` เองทุกครั้ง ไฟล์นั้นถูก generate

## สถาปัตยกรรม

### ชั้นของโค้ด — สูตรบริสุทธิ์ vs. query

| ชั้น | ไฟล์ | แตะ DB |
|---|---|---|
| สูตร (pure) | `lib/nutrition.ts`, `lib/dates.ts` | ❌ |
| ตรวจข้อมูล | `lib/validation.ts` (zod ใช้ร่วม client + server) | ❌ |
| query + รวมยอด | `lib/diary.ts` | ✅ |

**สูตรทุกตัวต้องอยู่ในชั้น pure และมี unit test ครอบ** — ห้ามเขียนสูตรลงใน SQL หรือใน component
การรวมยอดรายวันทำใน JavaScript (ดึงแถวดิบมารวมในหน่วยความจำ) โดยตั้งใจ
เพื่อไม่ให้มีสูตรชุดที่สองใน SQL ที่ไม่มีเทสต์ครอบ

### env และ db เป็น lazy โดยตั้งใจ

`serverEnv()` (`src/env.ts`) ตรวจ env ตอนเรียกครั้งแรก ไม่ใช่ตอน import และ `db` (`src/db/index.ts`)
เป็น Proxy ที่ต่อฐานข้อมูลตอนเรียกเมธอดแรก — เพราะ `next build` ต้อง import ทุก route
ถ้าอ่าน env ตอน import แอปจะ build ไม่ผ่านบนเครื่องที่ไม่มี `DATABASE_URL` (เช่น CI)
**อย่าเปลี่ยนเป็น top-level validation หรือ top-level `postgres()`**

`prepare: false` จำเป็นสำหรับ transaction pooler ของ Supabase (port 6543) ห้ามเอาออก

**สอง connection string คนละหน้าที่** — ตัวแอปใช้ `DATABASE_URL` (transaction pooler, 6543)
ส่วน migration ใช้ `DIRECT_URL` (session pooler, 5432) เพราะ transaction pooler
ไม่การันตีว่าทุกคำสั่งใน migration เดียวกันจะวิ่งบน connection เดิม DDL ยาว ๆ จึงพังกลางทาง

### เขตเวลา

ตัดวัน/เดือนด้วย `APP_TIMEZONE` (`Asia/Bangkok`) เสมอ ผ่าน `lib/dates.ts` — ห้ามใช้
`new Date().toISOString().slice(0,10)` หรือเวลาเครื่อง (เซิร์ฟเวอร์ Vercel เป็น UTC
ถ้าใช้เวลาเครื่อง มื้อหลังเที่ยงคืนถึงตีเจ็ดจะไปโผล่ผิดวัน)

## ระบบสี — สีสดสงวนไว้ให้ข้อมูลเท่านั้น

โทเคนทั้งหมดอยู่ใน `globals.css` แบ่งเป็นสองกลุ่มที่ห้ามปนกัน

| กลุ่ม | โทเคน | ใช้กับ |
|---|---|---|
| กระดาษ/หมึก | `paper` `surface` `sunken` `ink` `ink-2` `ink-3` `line` `line-strong` | ทุกอย่างที่เป็นส่วนของหน้าจอ — ปุ่ม เมนู ขอบ พื้นหลัง |
| สีข้อมูล | `protein` `carb` `fat` + `good` `critical` | เฉพาะแถบวัด จุดสี และสถานะ |

**ปุ่มหลักเป็นสีหมึก ไม่ใช่สีแบรนด์สดใส** ถ้าปุ่มก็สี เมนูก็สี แถบสารอาหารก็สี
สายตาจะไม่รู้ว่าสีไหนแปลว่าอะไร — พอมีสีอยู่แค่สามจุดในหน้า สมองจะจับได้ทันทีว่าสีคือข้อมูล
ผลพลอยได้คือคอนทราสต์: ขาวบนหมึกผ่าน WCAG สบาย ๆ ต่างจากขาวบนสีสดที่มักไม่ผ่าน

**ห้ามเปลี่ยนสีสารอาหารทีละตัวโดยไม่รันตัวตรวจ** ชุดนี้ผ่าน `validate_palette.js`
ของ skill `dataviz` ทั้งสองโหมด (ระยะห่างสำหรับคนตาบอดสีและคนสายตาปกติ)
ชุดที่เคยลองใช้ม่วงแทนส้มผ่านโหมดสว่างแต่พังในโหมดมืด — ม่วงกับน้ำเงินห่างกันแค่ ΔE 1.9

**จุดสีต้องมีชื่อกำกับเสมอ** ห้ามให้สีเป็นตัวบอกความหมายลำพัง (ไขมันสีเขียวน้ำทะเล
มีคอนทราสต์ต่ำกว่า 3:1 บนพื้นสว่าง ตัวหนังสือกำกับคือสิ่งที่ทำให้ยังอ่านได้)

ฟอนต์คือ **IBM Plex Sans Thai** ผ่าน `next/font` — เลือกเพราะอักษรไทยถูกออกแบบมา
พร้อมกับละตินและตัวเลข ไม่ใช่ฟอนต์ฝรั่งที่เอาไทยมาแปะ ตัวเลขทุกที่ใช้คลาส `.tnum`
ให้ความกว้างเท่ากัน ไม่งั้นยอดที่นับขึ้นจะกระตุกซ้ายขวา

## หน้าจอและจุดตัดขนาดจอ

ออกแบบมือถือก่อนเสมอ แล้วค่อยขยายขึ้น จุดตัดที่ใช้จริงมีสามจุด

| ขนาด | เมนู | เนื้อหา |
|---|---|---|
| < 1024 | แถบล่าง (นิ้วโป้งเอื้อมถึง) | คอลัมน์เดียว กว้างสุด `max-w-2xl` ที่ ≥ 768 |
| ≥ 1024 (`lg`) | แถบข้างซ้าย กว้าง 14rem | คอลัมน์เดียว |
| ≥ 1280 (`xl`) | แถบข้างซ้าย | สองคอลัมน์ — ยอดรวมตรึงไว้ขวา |

**แถบข้างเริ่มที่ `lg` ไม่ใช่ `md`** — ที่ 768 แถบข้างกินไป 224 px เหลือเนื้อหา 544 px
ซึ่งแคบกว่าตอนไม่มีแถบข้างเสียอีก และแท็บเล็ตเป็นจอสัมผัส แถบล่างเหมาะกว่าอยู่แล้ว

**แยกสองคอลัมน์ที่ `xl` ไม่ใช่ `lg`** — ที่ 1024 ความกว้างที่เหลือหลังหักแถบข้างคือ 800 px
แบ่งสองคอลัมน์แล้วคอลัมน์รายการอาหารจะเหลือ ~420 px ซึ่งอึดอัดกว่าเรียงลงมาตรง ๆ

ปุ่มและช่องกรอกสูงอย่างน้อย 44 px, `input` ขนาด 16 px (กัน iOS ซูมหน้าเอง),
เส้นโฟกัสวาดด้วย `:focus-visible` ใน `globals.css` **ห้ามใส่ `outline-none` โดยไม่มีตัวแทน**

## ข้อมูลอาหาร

คลังอาหารมาจาก **USDA FoodData Central — SR Legacy** (7,793 รายการ ค่าจากห้องแล็บ)
ไฟล์ต้นทางไม่เก็บใน repo เพราะ 201 MB โหลดใหม่ได้จาก
`https://fdc.nal.usda.gov/fdc-datasets/FoodData_Central_sr_legacy_food_json_2021-10-28.zip`
แล้วรัน `npm run import:usda -- <path ของไฟล์ json>` (รันซ้ำได้ upsert ตาม source + source_ref)

- **กฎการแปลงอยู่ใน `lib/usda.ts` เท่านั้น** สคริปต์ import เป็นแค่คนป้อนข้อมูล
  ห้ามเขียนตรรกะแปลงหน่วยลงในสคริปต์ เพราะเทสต์ครอบเฉพาะชั้น lib
- **ชื่อไทยอยู่ใน `db/thai-names.ts` แมปด้วย description ของ USDA แบบตรงตัวอักษร**
  ไม่ใช่ fdcId — สะกดผิดแล้วสคริปต์จะฟ้องว่า "หาคู่ไม่เจอ" ส่วน fdcId ที่ผิดจะเงียบ
- **เลือกแถวที่ไม่ใส่เกลือเสมอเมื่อมีให้เลือก** USDA มีทั้ง "with salt" และไม่ใส่
  ข้าวสวยแถวที่ใส่เกลือมีโซเดียม 382 มก./100 ก. เทียบกับ 1 มก. ของแถวที่ไม่ใส่
  หยิบผิดแถว = ยอดโซเดียมทั้งวันของผู้ใช้เพี้ยนไปเป็นเท่าตัว
- ไมโครเก็บได้เฉลี่ย 12.9 จาก 14 ตัวต่อรายการ คีย์ที่ขาดคือ "ไม่มีข้อมูล" ไม่ใช่ศูนย์

### บาร์โค้ด (Open Food Facts)

สินค้าสำเร็จรูปดึงจาก **Open Food Facts** ตอนสแกน ไม่ได้ import ทั้งฐาน (มีเป็นล้านรายการ
แต่คนหนึ่งคนสแกนจริงไม่กี่ร้อยชิ้น) ผลที่ดึงมาแล้วถูกบันทึกลง `foods` ถาวร ครั้งต่อไปไม่ยิงเน็ตอีก

- **`verified = false` เสมอ** เพราะเป็นข้อมูลที่ผู้ใช้ทั่วโลกกรอกเอง ไม่ใช่ค่าจากห้องแล็บแบบ USDA
- **`lib/off.ts` ตรวจความสมเหตุสมผลก่อนรับเข้าคลัง** ทิ้งรายการที่พลังงานเกิน 900 kcal/100 ก.
  หรือมาโครเกิน 100 ก./100 ก. เพราะคนกรอกมักใส่ค่าต่อชิ้นลงในช่องต่อ 100 กรัม
- **หน่วยของ OFF เป็นกรัมทั้งหมด** (โซเดียม 0.045 = 45 มก.) ต้องแปลงผ่าน `lib/units.ts`
  และ `roundNutrient()` เก็บทศนิยม 4 ตำแหน่งเมื่อค่าน้อยกว่า 1 ไม่งั้นวิตามินจะถูกปัดเป็น 0
  แล้วอ่านผิดว่า "ไม่มีสารอาหารตัวนี้"
- `BarcodeDetector` ยังไม่มีใน Safari บน iOS **ช่องกรอกเลขเองต้องมีเสมอ** ไม่ใช่ทางเลือกสำรอง

## PWA

`manifest.ts` เป็น metadata route ของ Next (ออกมาเป็น `/manifest.webmanifest`)
ไอคอนสร้างจากโค้ดด้วย `npm run icons` (`scripts/generate-icons.mjs` เขียน PNG เองด้วย zlib
ไม่ต้องลง sharp) ไฟล์ผลลัพธ์ commit ไว้ใน `public/` เพื่อให้ build ไม่ต้องรันสคริปต์

- **service worker ห้ามแคชหน้าที่ต้องล็อกอินหรือคำตอบของ API** เครื่องเดียวอาจมีคนใช้หลายคน
  ถ้าแคชไว้ คนถัดไปที่เปิดแอปจะเห็นข้อมูลของคนก่อนหน้าโดยเซิร์ฟเวอร์ไม่รู้เรื่องด้วย
  แคชได้เฉพาะ `/offline`, ไอคอน และบันเดิลใน `/_next/static/` (ชื่อมีแฮชอยู่แล้ว)
- **หน้า `/offline` ต้องไม่แตะฐานข้อมูลและไม่ต้องล็อกอิน** ไม่งั้นตอนออฟไลน์จะเรนเดอร์ไม่ได้
- `PwaRegister` ข้ามการลงทะเบียนตอน dev เพราะ service worker จะแคชบันเดิลเก่าค้างจน HMR ไม่ทำงาน
- ทดสอบ PWA ต้องใช้ production build (`npm run build` แล้ว preview ชื่อ `bodymefit-prod` ที่พอร์ต 3001)

## เป้าที่ปรับตามร่างกายจริง

`lib/adaptive.ts` ประมาณ TDEE จากสมดุลพลังงาน (กินเฉลี่ยเท่าไร เทียบกับน้ำหนักที่เปลี่ยน)
แล้วผสมกับค่าจากสูตรตามปริมาณข้อมูลที่มี — ชั้น pure ล้วน มีเทสต์ครอบ

ป้องกันไว้สามชั้น **ห้ามถอดออกชั้นใดชั้นหนึ่ง**

1. ต้องบันทึกอาหารครบ 70% ของช่วงก่อน ไม่งั้นค่าเฉลี่ยที่กินต่ำกว่าความจริง
   แล้วระบบจะสรุปว่าเผาผลาญน้อยกว่าจริง และตั้งเป้าต่ำเกินไปจนอันตราย
2. เชื่อค่าที่วัดได้ทีละน้อยจาก 14 ถึง 28 วัน ไม่สลับทันที เพราะเป้าที่กระโดด
   300 kcal ข้ามคืนทำให้ผู้ใช้เลิกเชื่อถือระบบ
3. จำกัดไม่ให้ต่างจากสูตรเกิน 35% กันข้อมูลผิดเพี้ยน (ชั่งตอนใส่เสื้อหนา ลืมบันทึกมื้อใหญ่)

`getIntakeStats()` นับ "วันที่บันทึก" แยกจาก "ความยาวช่วง" โดยตั้งใจ — ห้ามเอาพลังงาน
ทั้งหมดหารด้วยจำนวนวันทั้งช่วง เพราะวันที่ลืมบันทึกจะถูกนับเป็นกินศูนย์แคล

น้ำหนักใช้ **เส้นแนวโน้ม (EWMA ค่าถ่วง 0.1)** ไม่ใช่ตัวเลขดิบ ทั้งในการแสดงผลและในสูตร
เพราะน้ำหนักรายวันแกว่งจากน้ำและเกลือได้เป็นกิโล ซึ่งกลบการเปลี่ยนแปลงจริงสัปดาห์ละ 0.5 กก.

## สูตรของผู้ใช้

`recipes` + `recipe_items` เก็บเป็น**ส่วนประกอบ ไม่ใช่ snapshot** ต่างจาก `diary_entries`
เพราะสูตรคือ "แบบ" ที่ผู้ใช้แก้ได้ ถ้าแก้ปริมาณแล้วตัวเลขไม่เปลี่ยนก็ไม่มีประโยชน์ที่จะแก้
ส่วนมื้อที่บันทึกลงไดอารีไปแล้วยังตรึงตัวเลขเดิมไว้ ไม่ขยับตามสูตรที่แก้ทีหลัง

`recipe_items.food_id` ใช้ `ON DELETE restrict` ไม่ใช่ cascade — ลบอาหารที่ยังถูกใช้ในสูตร
ไม่ได้ ไม่งั้นสูตรจะคำนวณไม่ได้ทั้งสูตรโดยผู้ใช้ไม่รู้ตัว

## กฎที่แก้แล้วพัง

- **`diary_entries` เก็บตัวเลขสารอาหารเป็น snapshot ตอนบันทึก** ห้าม join สดจาก `foods` ตอนแสดงผล
  — ถ้า join สด วันที่แก้ข้อมูลอาหารหนึ่งรายการ ประวัติย้อนหลังทั้งหมดจะเปลี่ยนตัวเลขตาม
  และผู้ใช้จะเห็นยอดของเมื่อวานขยับเอง
- **เป้าหมายรายวันถูกตรึงลง `daily_targets` พร้อม `basis` (jsonb) ครั้งเดียวต่อวัน**
  ห้ามเปลี่ยนเป็นคำนวณสด — น้ำหนักตัวเปลี่ยนทุกสัปดาห์ ถ้าคำนวณสดกราฟย้อนหลังจะเพี้ยนทั้งแถบ
- **ไมโครเก็บเป็น `jsonb` คอลัมน์เดียว** ไม่ใช่คอลัมน์ละสารอาหาร — เพิ่มสารอาหารใหม่ไม่ต้อง migrate
  และแยก "ไม่มีข้อมูล" (คีย์หาย) ออกจาก "มีค่าเป็นศูนย์" ได้
- **ค่าสารอาหารใน `foods` อ้างต่อ 100 กรัมเสมอ** หน่วยครัว ("1 ถ้วย") อยู่ในตาราง `food_servings`
  แยกออกมา ห้ามยัดลง `foods`
- **ทุก query กรองด้วย `user_id` เสมอ** ฐานข้อมูลเปิด RLS แบบ deny-all ทุกตารางที่มีข้อมูลผู้ใช้
  เข้าถึงผ่าน server เท่านั้น ตารางใหม่ต้องมี migration เปิด RLS ด้วย
- **การลบใช้ `and(eq(id), eq(userId))` ในคำสั่งเดียว** ห้าม select ก่อนแล้วค่อยลบ (กัน race)
  และตอบ 404 เหมือนกันทั้งกรณี "ไม่มี" และ "ไม่ใช่ของคุณ"
- **server เป็นเจ้าของตัวเลข** — API ที่มี `foodId` คำนวณสารอาหารเองจากฐานข้อมูล
  และเมินค่าที่ client ส่งมา รวมถึงตอน**แก้ไข**รายการเดิมด้วย ไม่ใช่แค่ตอนเพิ่ม
- **ปลายทางที่ยิงต่อไปยังบริการภายนอกต้องมีเพดานการเรียก** (`lib/rate-limit.ts`)
  ไม่งั้นผู้ใช้ที่ล็อกอินแล้วยิงรัวได้ไม่จำกัด = เราไปถล่มบริการของคนอื่นในนามแอปเรา
  เพดานนี้เก็บในหน่วยความจำของ process จึงเป็น "ต่อ instance" ไม่ใช่ทั้งระบบ
